import { paymentsRequestsRouter } from '@infrastructure/webserver/routes/payments-requests';
import Router from 'koa-router';

export const paymentsRouter = new Router();
// .get('/')
// .post('/')
// .get('/:paymentId')
// .use(
//     '/requests',
//     paymentsRequestsRouter.routes(),
//     paymentsRequestsRouter.allowedMethods(),
// );
