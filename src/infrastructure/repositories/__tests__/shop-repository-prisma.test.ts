describe('shop-repository - prisma', function () {
    describe('persist()', function () {
        test.concurrent('persist a shop to database', async () => {
            // Given

            // When

            // Then
            expect(true).toEqual(true); // FIXME
        });

        test.concurrent('throw if the shop handle already exist', async () => {
            // Given

            // When

            // Then
            expect(true).toEqual(true); // FIXME
        });
    });

    describe('merge()', function () {
        test.concurrent('merge with an existing shop', async () => {
            // Given

            // When

            // Then
            expect(true).toEqual(true); // FIXME
        });

        test.concurrent('throw if the shop doesnt exist', async () => {
            // Given

            // When

            // Then
            expect(true).toEqual(true); // FIXME
        });
    });

    describe('findByHandle()', function () {
        test.concurrent('returns entity of an existing shop', async () => {
            // Given

            // When

            // Then
            expect(true).toEqual(true); // FIXME
        });

        test.concurrent('returns null if no shop exist', async () => {
            // Given

            // When

            // Then
            expect(true).toEqual(true); // FIXME
        });
    });
});
